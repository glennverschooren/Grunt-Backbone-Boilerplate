//
// CONFIG --------------------------------------------------------------------
//

// Define config namespace
var Config = Config || (Config = {});

// Backend location - URL
// where the API is running
Config.SERVER = 'http://yourREST';